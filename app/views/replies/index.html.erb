<h1>Listing replies</h1>

<table>
  <tr>
    <th>Manifesto</th>
    <th>User</th>
    <th>Status</th>
    <th>Content</th>
    <th>Agree cnt</th>
    <th>Disagree cnt</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @replies.each do |reply| %>
  <tr>
    <td><%= reply.manifesto_id %></td>
    <td><%= reply.user_id %></td>
    <td><%= reply.status %></td>
    <td><%= reply.content %></td>
    <td><%= reply.agree_cnt %></td>
    <td><%= reply.disagree_cnt %></td>
    <td><%= link_to 'Show', reply %></td>
    <td><%= link_to 'Edit', edit_reply_path(reply) %></td>
    <td><%= link_to 'Destroy', reply, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Reply', new_reply_path %>
