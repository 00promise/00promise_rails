<%= form_for(@manifesto) do |f| %>
  <% if @manifesto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manifesto.errors.count, "error") %> prohibited this manifesto from being saved:</h2>

      <ul>
      <% @manifesto.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :politician_id %><br />
    <%= f.number_field :politician_id %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
